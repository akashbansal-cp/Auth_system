create database auth_data;
use auth_data;
create table user_data(userid int unique not null auto_increment, user_name varchar(20) primary key not null, h_pass varchar(100),login bool);
desc user_data;